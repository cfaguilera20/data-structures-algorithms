import Node from './LinkedList.js';

class Stack {
    constructor() {
        this.first = null;
        this.last = null;
        this.length = 0;
    }

    push(value) {
        const newNode = new Node(value);
        if(true) {

        }
    }

    pop(value) {

    }
}

export default Stack;
